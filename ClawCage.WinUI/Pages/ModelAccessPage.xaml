<Page x:Class="ClawCage.WinUI.Pages.ModelAccessPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:components="using:ClawCage.WinUI.Components"
      Background="Transparent">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ── Page Header ── -->
        <Grid Padding="24,20,24,16" ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Spacing="3">
                <TextBlock FontSize="26" FontWeight="SemiBold" Text="模型接入"/>
                <TextBlock FontSize="13" Opacity="0.6" Text="管理 AI 提供商及其模型配置"/>
                <StackPanel Orientation="Horizontal" Spacing="12">
                    <TextBlock x:Name="ModeText" FontSize="12" Opacity="0.65" Text="Mode: -"/>
                    <TextBlock x:Name="StatusText" FontSize="12" Opacity="0.65" Text="就绪"/>
                </StackPanel>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Bottom" Spacing="10">
                <Button Content="增加模型" Click="AddModelButton_Click" MinWidth="92" VerticalAlignment="Center"/>
                <Button Content="刷新" Click="ReloadButton_Click" MinWidth="72" VerticalAlignment="Center"/>
            </StackPanel>
        </Grid>

        <!-- Header Divider -->
        <Rectangle Grid.Row="1" Height="1" Fill="{ThemeResource DividerStrokeColorDefaultBrush}"/>

        <!-- ── Provider List ── -->
        <ScrollViewer Grid.Row="2" Padding="24,16,24,24">
            <ItemsControl ItemsSource="{x:Bind ProviderItems, Mode=OneWay}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Spacing="12"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    </Style>
                </ItemsControl.ItemContainerStyle>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <components:ProviderCard
                            Item="{Binding}"
                            EditProviderRequested="ProviderCard_EditProviderRequested"
                            EditModelRequested="ProviderCard_EditModelRequested"/>

                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

    </Grid>
</Page>
